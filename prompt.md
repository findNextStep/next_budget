计划编写一个记账软件，ui和逻辑代码分离 
逻辑代码部分，设计一个记录方式，需要记录长期记账的账目，账目内容包含 
时间，类型（包括主食，饮料，零食，房租，薪水等类型，需要易扩展），收入/支出数值 
能对外提供日/周/月/年维度的支出统计 
具有稳定收入模块，通过给定的月收入，根据每月的天数转化为日薪（每日12 点自动入账，需要考虑可能在对应时间app没打开的场景） 
能够根据给定的收入/支出数值，通过历史最近发生的同数额交易判断类型 
ui代码部分，主界面可以通过+-两个按钮进入记账模块，内置数字输入器 
通过界面切换可以进入各个维度的数据统计，可以在日/周/月/年维度下，统计各个类型下在 每日的开销 
可以设置月收入或者日收入用于逻辑模块记录日薪 
可以通过浮动窗口快速记录支出 
ui可以设置为白色/黑色/纯黑模式（适用于oled屏幕）
支持读取csv文件，将文件中的数据导入到账目记录中,csv文件格式参考example.csv
支持导出账目记录到csv文件

